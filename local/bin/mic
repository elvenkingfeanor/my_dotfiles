#!/bin/sh
#mic
#if [[ $(/usr/bin/amixer get Capture | /usr/bin/grep -o off | /usr/bin/uniq -d) == "off" ]]; then /usr/bin/echo "off"; else /usr/bin/echo "on"; fi
if [[ $(/usr/bin/wpctl get-volume @DEFAULT_AUDIO_SOURCE@ | /usr/bin/awk '{print $NF}') == '[MUTED]' ]]; then
	/usr/bin/echo "off"; else
	/usr/bin/echo "$(/usr/bin/wpctl get-volume @DEFAULT_AUDIO_SOURCE@ | /usr/bin/awk '{print $NF}')*100" | /usr/bin/bc | /usr/bin/sed 's/\..*$//'
fi
